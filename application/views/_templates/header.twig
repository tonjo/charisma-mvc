    <!-- topbar starts -->
    <div class="navbar navbar-default" role="navigation">

        <div class="navbar-inner">
            <button type="button" class="navbar-toggle pull-left animated flip">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

            <a class="navbar-brand" href="{{ constant('URL') }}"> <img alt="Charisma Logo" src="{{ constant('URL') }}public/img/logo20.png" class="hidden-xs"/>
                <span style="font-size:11px">Charisma MVC</span></a>

            <ul class="collapse navbar-collapse nav navbar-nav top-menu">
                <li><a href="#"><i class="glyphicon glyphicon-globe"></i> Menu link</a></li>

            </ul>

            <!-- user dropdown starts -->
            <div class="btn-group pull-right">
                <button class="btn btn-default dropdown-toggle" data-toggle="dropdown">
                    <i class="glyphicon glyphicon-user"></i>
                    <span class="hidden-xs">{{ user_name }}</span>
                    <span class="caret"></span>
                </button>
                <ul class="dropdown-menu">
                    {% if is_admin %}
                        <li><a href="{{ constant('URL') }}admin/users">Utenti</a></li>
                        <li><a href="{{ constant('URL') }}admin/access_log">Log accessi</a></li>
                        <li class="divider"></li>
                    {% endif %}
                    <li><a href="{{ constant('URL') }}admin/change_psw">Cambia password</a></li>
                    <li><a href="{{ constant('URL') }}login/logout">Logout</a></li>
                </ul>
            </div>
            <!-- user dropdown ends -->


        </div>
    </div>
    <!-- topbar ends -->
